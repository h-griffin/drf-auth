version: '3.7'

services:
  web:
    build: .
    # command: python manage.py runserver 0.0.0.0:8000
    command: gunicorn tank_project.wsgi:aplication --bind 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8001:8000"
#     depends_on: 
#       - db
#   db:
#     image: postgres:11
#     volumes:
#         - postgres_data:/var/lib/postgresql/data/
#     environment: 
#       POSTGRES_PASSWORD: postgres

# volumes:
#     postgres_data:





  
# run with runserver then docker then gunicorn then sql

